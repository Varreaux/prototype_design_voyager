# API keys — never commit these
.env

# Mechanic libraries (generated data, optional to commit)
library.json
library_card.json

# Python cache
__pycache__/
*.py[cod]
*.pyo

# macOS
.DS_Store

# Claude session files
.claude/
work_done_so_far.md

# Personal markdown notes (already-tracked .md files like README.md
# and SESSION_HANDOFF.md remain tracked; this only stops new ones)
*.md
